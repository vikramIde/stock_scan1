# Quasar App

> A Quasar project

## Build Setup

``` bash
# install dependencies
$ npm install

# build for production with minification
$ quasar build

$ quasar wrap cordova
$ cd cordova
$ cordova platform add android
$ cordova run
```
